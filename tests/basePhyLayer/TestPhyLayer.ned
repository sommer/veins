package org.mixim.tests.basePhyLayer;
import org.mixim.base.phyLayer.BasePhyLayer;

simple TestPhyLayer extends BasePhyLayer
{
    parameters:
    	@class(TestPhyLayer);
    	int protocol = default(0);
}

